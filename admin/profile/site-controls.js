import{publisherRequest,isPublisherReady,onAuthChange}from'./auth.js';
import{isDraftMode,stageSettings,stageSeo}from'./draft.js';
const $=id=>document.getElementById(id);const localAssets={};let draftImage='',draftColors=null,currentSettings=null;
function formatDate(v){try{return new Intl.DateTimeFormat(undefined,{dateStyle:'medium',timeStyle:'short'}).format(new Date(v))}catch{return v||'Unknown time'}}function short(v,max=72){const s=String(v||'');return s.length>max?`${s.slice(0,max-1)}…`:s}
function setStatus(m,t=''){const e=$('settingsStatus');e.textContent=m;e.className=`settings-status${t?` ${t}`:''}`}function setSeoStatus(m,t=''){const e=$('seoStatus');e.textContent=m;e.className=`settings-status${t?` ${t}`:''}`}
export function settingsPayload(){const total=Math.max(1,Math.min(12,Number($('commissionTotal').value)||4));return{bio:$('bioInput').value.trim(),statusText:$('statusInput').value.trim(),commissionOpen:$('commissionOpen').checked,commissionTotal:total,commissionUsed:Math.max(0,Math.min(total,Number($('commissionUsed').value)||0)),backgroundEnabled:$('backgroundEnabled').checked,bannerEnabled:$('bannerEnabled').checked,faviconEnabled:$('faviconEnabled').checked,themeMode:$('themeMode').value==='manual'?'manual':'auto',mainColor:$('mainColorInput').value,secondaryColor:$('secondaryColorInput').value,accentColor:$('accentColorInput').value,maintenanceEnabled:$('maintenanceEnabled').checked,maintenanceTitle:$('maintenanceTitle').value.trim(),maintenanceMessage:$('maintenanceMessage').value.trim()}}
function fillSettings(s){currentSettings=s||{};$('bioInput').value=currentSettings.bio||'';$('statusInput').value=currentSettings.statusText||'';$('commissionOpen').checked=Boolean(currentSettings.commissionOpen);$('commissionTotal').value=currentSettings.commissionTotal||4;$('commissionUsed').value=currentSettings.commissionUsed||0;$('backgroundEnabled').checked=Boolean(currentSettings.backgroundEnabled);$('bannerEnabled').checked=Boolean(currentSettings.bannerEnabled);$('faviconEnabled').checked=Boolean(currentSettings.faviconEnabled);$('themeMode').value=currentSettings.themeMode==='manual'?'manual':'auto';$('mainColorInput').value=currentSettings.mainColor||'#ffffff';$('secondaryColorInput').value=currentSettings.secondaryColor||'#9f9f9f';$('accentColorInput').value=currentSettings.accentColor||'#d7d7d7';$('maintenanceEnabled').checked=Boolean(currentSettings.maintenanceEnabled);$('maintenanceTitle').value=currentSettings.maintenanceTitle||'Back soon';$('maintenanceMessage').value=currentSettings.maintenanceMessage||'The site is being updated right now.';toggleThemeInputs();applyRealPreview()}
function toggleThemeInputs(){$('manualThemeFields').hidden=$('themeMode').value!=='manual'}
export async function loadSettings(){if(!isPublisherReady())return;try{const r=await publisherRequest('/settings');fillSettings(r.settings||{})}catch(e){setStatus(e.message,'error')}}
export async function saveSettings(silent=false){if(!isPublisherReady())return;const payload=settingsPayload();if(isDraftMode()){stageSettings(payload,'Site content, theme & availability');currentSettings=payload;applyRealPreview();if(!silent)setStatus('Site settings staged. Publish All will send them in the combined commit.','success');return payload}if(!silent){$('saveSettingsButton').disabled=true;$('saveSettingsButton').innerHTML='<i class="bx bx-loader-alt bx-spin"></i> Saving…'}try{const r=await publisherRequest('/settings',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});fillSettings(r.settings||payload);if(!silent)setStatus('Site settings saved directly.','success');loadActivity()}catch(e){if(!silent)setStatus(e.message||'Could not save settings.','error');throw e}finally{if(!silent){$('saveSettingsButton').disabled=false;$('saveSettingsButton').innerHTML='<i class="bx bx-save"></i> Save site settings'}}return payload}
export function setAssetPreview(kind,url){if(localAssets[kind]?.startsWith('blob:'))URL.revokeObjectURL(localAssets[kind]);url?localAssets[kind]=url:delete localAssets[kind];applyRealPreview()}
function maintenancePreview(doc,settings){let layer=doc.getElementById('adminMaintenancePreview');if(settings.maintenanceEnabled){if(!layer){layer=doc.createElement('div');layer.id='adminMaintenancePreview';doc.body.append(layer)}Object.assign(layer.style,{position:'fixed',inset:'0',zIndex:'999999',display:'grid',placeItems:'center',padding:'28px',background:'rgba(5,5,5,.96)',color:'#f1f1f1',textAlign:'center',fontFamily:'inherit'});layer.innerHTML=`<div style="max-width:560px"><small style="opacity:.55;letter-spacing:.16em;text-transform:uppercase">Immortal</small><h1 style="font-size:clamp(2rem,7vw,4.4rem);margin:.5rem 0 1rem">${settings.maintenanceTitle||'Back soon'}</h1><p style="opacity:.68;line-height:1.7">${settings.maintenanceMessage||''}</p></div>`}else layer?.remove()}
export function applyRealPreview(next={}){if(next.image!==undefined)draftImage=next.image||'';if(next.colors!==undefined)draftColors=next.colors||null;const frame=$('realPreviewFrame'),doc=frame?.contentDocument;if(!doc)return;const settings=settingsPayload(),profile=doc.querySelector('.profile-img');if(profile)profile.src=draftImage||`/assets/pfp.jpg?v=${Date.now()}`;if(settings.themeMode==='manual'){doc.documentElement.style.setProperty('--main-color',settings.mainColor,'important');doc.documentElement.style.setProperty('--secondary-color',settings.secondaryColor,'important');doc.documentElement.style.setProperty('--accent-color',settings.accentColor,'important')}else if(draftColors){doc.documentElement.style.setProperty('--main-color',draftColors.main);doc.documentElement.style.setProperty('--secondary-color',draftColors.secondary);doc.documentElement.style.setProperty('--accent-color',draftColors.accent)}const bio=doc.querySelector('.tagline.enhanced-tagline');if(bio&&settings.bio)bio.textContent=settings.bio;const status=doc.querySelector('.status-card .label');if(status&&settings.statusText)status.textContent=settings.statusText;const commission=doc.querySelector('.commission-cta .btn-text');if(commission)commission.textContent=settings.commissionOpen?'Commission':'Commissions Closed';const home=doc.querySelector('.home');if(home){const bg=localAssets.background||`/assets/site-background.jpg?v=${Date.now()}`;if(settings.backgroundEnabled){home.style.backgroundImage=`linear-gradient(rgba(5,5,5,.62),rgba(5,5,5,.82)),url("${bg}")`;home.style.backgroundSize='cover';home.style.backgroundPosition='center'}else home.style.removeProperty('background-image');let banner=home.querySelector(':scope > .admin-preview-banner');if(settings.bannerEnabled){if(!banner){banner=doc.createElement('div');banner.className='admin-preview-banner';home.prepend(banner)}Object.assign(banner.style,{position:'absolute',inset:'0',pointerEvents:'none',zIndex:'0',opacity:'.16',backgroundImage:`url("${localAssets.banner||`/assets/site-banner.jpg?v=${Date.now()}`}"`,backgroundSize:'cover',backgroundPosition:'center',maskImage:'linear-gradient(to bottom,black,transparent 72%)'})}else banner?.remove()}maintenancePreview(doc,settings)}
export async function loadActivity(){if(!isPublisherReady())return;const box=$('activityList');box.innerHTML='<div class="history-empty">Loading activity…</div>';try{const{activity=[]}=await publisherRequest('/activity');box.replaceChildren();if(!activity.length){box.innerHTML='<div class="history-empty">No managed changes yet.</div>';return}for(const item of activity){const icon=/profile/i.test(item.message)?'bx-user-circle':/SEO/i.test(item.message)?'bx-search':/snapshot/i.test(item.message)?'bx-archive':/background|banner|favicon/i.test(item.message)?'bx-image':'bx-slider-alt',row=document.createElement('div');row.className='activity-item';row.innerHTML=`<span class="activity-icon"><i class="bx ${icon}"></i></span><div class="activity-copy"><strong>${short(item.message,64)}</strong><small>${formatDate(item.date)} · ${item.shortSha}</small></div>${item.commitUrl?`<a href="${item.commitUrl}" target="_blank" rel="noopener">Open</a>`:''}`;box.append(row)}}catch(e){box.innerHTML=`<div class="history-empty">${short(e.message)}</div>`}}
export async function loadSnapshots(){if(!isPublisherReady())return;const box=$('snapshotList');box.innerHTML='<div class="history-empty">Loading snapshots…</div>';try{const{snapshots=[]}=await publisherRequest('/snapshots');box.replaceChildren();if(!snapshots.length){box.innerHTML='<div class="history-empty">No snapshots yet.</div>';return}for(const item of snapshots){const row=document.createElement('div');row.className='snapshot-item';row.innerHTML=`<span class="activity-icon"><i class="bx bx-archive"></i></span><div class="snapshot-copy"><strong>${short(item.label,52)}</strong><small>${formatDate(item.createdAt)} · ${String(item.sha||'').slice(0,7)}</small></div><div class="snapshot-actions"><button type="button">Restore</button></div>`;row.querySelector('button').addEventListener('click',async()=>{if(!confirm(`Restore snapshot “${item.label}”?`))return;const b=row.querySelector('button');b.disabled=true;b.textContent='Restoring…';try{await publisherRequest('/snapshots/restore',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:item.id}),timeout:90000});await Promise.allSettled([loadSettings(),loadSeo(),loadActivity(),loadSnapshots()]);window.dispatchEvent(new CustomEvent('immortal:snapshot-restored'));setStatus('Snapshot restored.','success')}catch(e){setStatus(e.message||'Snapshot restore failed.','error')}finally{b.disabled=false;b.textContent='Restore'}});box.append(row)}}catch(e){box.innerHTML=`<div class="history-empty">${short(e.message)}</div>`}}
function updateSocialPreview(){$('socialPreviewTitle').textContent=$('seoTitleInput').value||'Immortal';$('socialPreviewDescription').textContent=$('seoDescriptionInput').value||'';$('socialPreviewImage').src=$('seoImageInput').value||'../../assets/Immortal-site-banner.png'}
function fillSeo(s){$('seoTitleInput').value=s.title||'';$('seoDescriptionInput').value=s.description||'';$('seoThemeColorInput').value=/^#[0-9a-f]{6}$/i.test(s.themeColor||'')?s.themeColor:'#0a0a0a';$('seoImageInput').value=s.image||'';updateSocialPreview()}
export async function loadSeo(){if(!isPublisherReady())return;try{const r=await publisherRequest('/seo');fillSeo(r.seo||{})}catch(e){setSeoStatus(e.message,'error')}}
function seoPayload(){return{title:$('seoTitleInput').value,description:$('seoDescriptionInput').value,themeColor:$('seoThemeColorInput').value,image:$('seoImageInput').value}}
async function saveSeo(){if(!isPublisherReady())return;const payload=seoPayload();if(isDraftMode()){stageSeo(payload,'SEO & social metadata');setSeoStatus('SEO changes staged for Publish All.','success');return}const b=$('saveSeoButton');b.disabled=true;b.innerHTML='<i class="bx bx-loader-alt bx-spin"></i> Saving…';try{const r=await publisherRequest('/seo',{method:'POST',timeout:30000,headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});fillSeo(r.seo||payload);setSeoStatus('Static title/meta/OG/Twitter tags updated.','success');loadActivity()}catch(e){setSeoStatus(e.message||'Could not update SEO metadata.','error')}finally{b.disabled=false;b.innerHTML='<i class="bx bx-save"></i> Save static SEO metadata'}}
export async function loadAllSiteData(){await Promise.allSettled([loadSettings(),loadActivity(),loadSnapshots(),loadSeo()])}
export function initSiteControls(){$('saveSettingsButton').addEventListener('click',()=>saveSettings());$('refreshActivity').addEventListener('click',loadActivity);$('createSnapshotButton').addEventListener('click',async()=>{const b=$('createSnapshotButton');b.disabled=true;b.innerHTML='<i class="bx bx-loader-alt bx-spin"></i> Saving…';try{await publisherRequest('/snapshots',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({label:$('snapshotLabel').value.trim()})});$('snapshotLabel').value='';await Promise.allSettled([loadSnapshots(),loadActivity()]);setStatus('Snapshot saved.','success')}catch(e){setStatus(e.message||'Could not save snapshot.','error')}finally{b.disabled=false;b.innerHTML='<i class="bx bx-camera"></i> Save snapshot'}});$('saveSeoButton').addEventListener('click',saveSeo);$('useBannerSeoButton').addEventListener('click',()=>{$('seoImageInput').value='https://xm5o.github.io/assets/site-banner.jpg';updateSocialPreview()});$('themeMode').addEventListener('change',()=>{toggleThemeInputs();applyRealPreview()});const fields=[$('bioInput'),$('statusInput'),$('commissionOpen'),$('commissionTotal'),$('commissionUsed'),$('backgroundEnabled'),$('bannerEnabled'),$('faviconEnabled'),$('themeMode'),$('mainColorInput'),$('secondaryColorInput'),$('accentColorInput'),$('maintenanceEnabled'),$('maintenanceTitle'),$('maintenanceMessage')];['input','change'].forEach(ev=>fields.forEach(el=>el.addEventListener(ev,applyRealPreview)));['input','change'].forEach(ev=>[$('seoTitleInput'),$('seoDescriptionInput'),$('seoThemeColorInput'),$('seoImageInput')].forEach(el=>el.addEventListener(ev,updateSocialPreview)));$('realPreviewFrame').addEventListener('load',()=>setTimeout(()=>applyRealPreview(),250));$('refreshRealPreview').addEventListener('click',()=>{$('realPreviewFrame').src=`../../?siteManagerPreview=${Date.now()}`});window.addEventListener('immortal:request-stage-settings',()=>saveSettings(true));window.addEventListener('immortal:reload-managed-state',loadAllSiteData);window.addEventListener('immortal:cms-published',loadAllSiteData);window.addEventListener('immortal:cms-undo',loadAllSiteData);onAuthChange(ready=>{if(ready)loadAllSiteData()})}
