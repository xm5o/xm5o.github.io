const settings = Object.freeze({
  slots: Object.freeze({
    enabled: true,
    total: 4,
    used: 0
  }),
  submission: Object.freeze({
    enabled: true,
    endpoint: ''
  })
});

export default settings;
